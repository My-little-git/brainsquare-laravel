<header class="auth-header">
    <div class="auth-container">
        <a href="{{ route('home') }}">
            <img src="{{ Vite::images('logo/logo.svg') }}" alt="" class="logo">
        </a>
    </div>
</header>
